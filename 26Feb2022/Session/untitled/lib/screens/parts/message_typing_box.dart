import 'package:flutter/material.dart';

class MessageTypingBox extends StatelessWidget {
  const MessageTypingBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 50,
      color: Colors.red,
    );
  }
}
